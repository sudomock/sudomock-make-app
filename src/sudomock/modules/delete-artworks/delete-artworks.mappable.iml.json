[
  {
    "name": "delete_by",
    "label": "Delete by",
    "type": "select",
    "options": [
      {
        "label": "URLs",
        "value": "urls"
      },
      {
        "label": "Mockup",
        "value": "mockup"
      }
    ],
    "required": true,
    "default": "urls"
  },
  {
    "name": "urls",
    "label": "URLs",
    "type": "array",
    "spec": {
      "type": "url",
      "label": "URL"
    },
    "required": true,
    "validate": {
      "minItems": 1
    },
    "visible": "{{parameters.delete_by == 'urls'}}"
  },
  {
    "name": "mockup_uuid",
    "label": "Mockup",
    "type": "select",
    "required": true,
    "mode": "edit",
    "options": "rpc://listMockups",
    "help": "Choose a mockup or map its UUID.",
    "visible": "{{parameters.delete_by == 'mockup'}}"
  }
]
