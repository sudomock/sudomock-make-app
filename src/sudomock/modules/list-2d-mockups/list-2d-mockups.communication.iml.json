{
  "url": "/sudoai/2d-mockups",
  "method": "GET",
  "qs": {
    "limit": 100,
    "offset": 0,
    "customizable_only": "{{if(parameters.customizable_only, true, undefined)}}"
  },
  "response": {
    "iterate": "{{body.data}}",
    "limit": "{{parameters.limit}}",
    "output": "{{item}}"
  },
  "pagination": {
    "qs": {
      "offset": "{{(pagination.page - 1) * 100}}"
    },
    "condition": "{{length(body.data) == 100}}"
  }
}
